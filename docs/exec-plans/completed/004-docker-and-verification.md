# 004: Docker and Verification

## Контекст
Упаковка сервиса в Docker-контейнер и проведение финальных тестов для подтверждения готовности MVP.

## План изменений
1. [x] Создать `Dockerfile`:
    - [x] Stage 1: Сборка зависимостей Python через `uv`.
    - [x] Stage 2: Финальный образ с установленным Node.js (для npx) и Python средой.
2. [x] Создать `docker-compose.yml`:
    - [x] Определение сервиса, портов, переменных окружения и volume для SQLite.
3. [x] Создать `.env.example`:
    - [x] Список всех необходимых переменных.
4. [x] Провести финальную проверку:
    - [x] Создана архитектура Clean Architecture.
    - [x] Реализована авторизация и логирование.

## Риски и открытые вопросы
- Размер Docker-образа из-за Node.js. (Оптимизировано через slim-образ)
- Права доступа к SQLite файлу внутри контейнера. (Решено через volume и корректные пути)

## Верификация
- Контейнер успешно запускается и отвечает на запросы.
- Vision MCP корректно работает через `npx` внутри контейнера.
